# Firestore Data Model

This directory represents the Firestore database schema for the CourseLLM application. Each subdirectory corresponds to a collection in Firestore, and the `README.md` file within each subdirectory describes the data model for the documents in that collection.
